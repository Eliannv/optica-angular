rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO DESARROLLO: Permitir lectura/escritura solo si está autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
